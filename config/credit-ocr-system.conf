# Credit OCR System Configuration
# Simple configuration file for production deployment

# AI Model Settings
generative_llm {
    url = "http://127.0.0.1:11435"
    model_name = "llama3.1:8b"
}

# Database Settings  
database {
    host = "localhost"
    port = 5432
    database = "dms_meta"
    username = "dms"
    password = "dms"
}

# Celery Broker Settings (Redis)
celery_broker {
    host = "localhost"
    port = 6379
}

# Storage Settings
storage {
    host = "localhost"
    port = 10000
    account_name = "devstoreaccount1"
}